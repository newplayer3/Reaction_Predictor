import torch 
import numpy as np
import re

class Digitalization():
    
    def __init__(self, Main, side, connection):
        self._connection = connection
        self._sidechain = side
        self._mainchain = Main
        self._digit = torch.Tensor(self._connection.count('[A-Z]', 10)
        
    
        
        
        
        
        
